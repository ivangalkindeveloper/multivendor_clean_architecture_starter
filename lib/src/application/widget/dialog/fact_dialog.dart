import 'package:mvs_widget/mvs_widget.dart';
import 'package:flutter/widgets.dart';

//TODO Starter: Dialog
class FactDialog<T> extends StatelessWidget with MVSDialogMixin<T> {
  const FactDialog({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
