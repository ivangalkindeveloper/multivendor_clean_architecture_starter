import 'package:flutter/widgets.dart';

//TODO Starter: PopUp
class FactPopUp extends StatelessWidget {
  const FactPopUp({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
